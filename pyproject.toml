[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "virelion-vb-101"
version = "0.1.0"
description = "Computational discovery and prioritization framework for myocardial regeneration gene-therapy candidates"
readme = "README.md"
license = {text = "GPL-3.0-or-later"}
authors = [
    {name = "Virelion Biotech", email = "contact@virelionbiotech.org"}
]
keywords = [
    "cardiac regeneration", "myocardial regeneration", "gene therapy",
    "multi-omics", "target discovery", "computational biology", "target prioritization"
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: GNU General Public License v3 or later (GPLv3+)",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Bio-Informatics",
    "Topic :: Scientific/Engineering :: Medical Science Apps.",
]
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.24",
    "pandas>=2.0",
    "scipy>=1.10",
    "pyyaml>=6.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.4",
    "pytest-cov>=4.1",
    "ruff>=0.16,<0.17",
    "mypy>=1.5",
    "black>=23.0",
]
analysis = [
    "scikit-learn>=1.3",
    "statsmodels>=0.14",
]
notebook = [
    "jupyter>=1.0",
    "ipykernel>=6.25",
]

[project.urls]
Homepage = "https://github.com/Virelion-Biotech/Virelion-VB-101"
Repository = "https://github.com/Virelion-Biotech/Virelion-VB-101"
Issues = "https://github.com/Virelion-Biotech/Virelion-VB-101/issues"

[project.scripts]
vb101 = "virelion_vb101.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --tb=short"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "B"]
ignore = ["B023"]
